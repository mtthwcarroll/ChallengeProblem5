Place the csv you want to convert in the same folder as the main.py
and run it. It will create an output folder where the csvs will be put. It can
also handle multiple csv, but the naming convention will be different so as
to avoid name conflicts.